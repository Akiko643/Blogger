import Editor from "@/components/editors/tiptap/Tiptap";

const TestEditor = () => {
    return (
        <div className="container">
            <Editor />;
        </div>
    );
};

export default TestEditor;
